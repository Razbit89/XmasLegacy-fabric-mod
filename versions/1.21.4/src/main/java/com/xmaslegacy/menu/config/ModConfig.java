package com.xmaslegacy.menu.config;

/**
 * Simple in-memory configuration for XmasLegacy mod settings.
 * Values are not persisted across game restarts (kept intentionally simple).
 */
public class ModConfig {
    public static boolean snowEnabled = true;
    public static int particleCount = 150;
    public static boolean tabSnowflakeEnabled = true;
}

# 멀티 프로젝트 마이그레이션 보고서 피드백

> 받으신 "완료 보고서" 내용을 보고 이상한 점들을 정리했습니다. 실제 저장소 구조를 눈으로 직접 확인하기 전까지는 100% 확신할 수 없는 부분들이라, "의심되는 지점" 위주로 적었습니다.

---

## 1. 제일 의심되는 부분 — 공통(common) 소스 폴더가 없음

보고서 내용:
> `versions/1.20.1/`, `versions/1.21.1/` 두 폴더에 **각각** 자바 코드와 리소스를 배치

**이게 왜 이상하냐면:**
1.20.1과 1.21.1은 마인크래프트 내부 API가 달라서 일부 코드(Mixin, 렌더링 관련 클래스)는 버전별로 따로 짜야 하는 게 맞습니다. **근데 색상 값, 설정 로직, 눈 파티클 계산식 같은 "마인크래프트 버전과 무관한 순수 로직"까지 두 폴더에 통째로 복붙되어 있다면 문제**예요.

**정상적인 멀티버전 Fabric 프로젝트 구조는 보통 이렇습니다:**
```
project-root/
├── common/                    ← 버전과 무관한 공통 코드 (설정, 색상 정의 등)
│   └── src/main/java/...
├── versions/
│   ├── 1.20.1/
│   │   └── src/main/java/...  ← 이 버전에서만 다른 Mixin/렌더 코드만
│   └── 1.21.1/
│       └── src/main/java/...  ← 이 버전에서만 다른 Mixin/렌더 코드만
```
`common` 모듈이 없고 두 버전 폴더에 전체 코드가 각각 다 들어있다면:
- 나중에 색상 하나 바꿀 때마다 **두 곳을 똑같이 고쳐야 함** (실수로 하나 빠뜨리면 버전마다 디자인이 달라지는 버그 발생)
- 유지보수 비용이 그대로 2배

**확인 요청**: `walkthrough.md`에 `common` 모듈에 대한 언급이 있는지, 아니면 정말로 전체 복붙인지 확인 필요.

---

## 2. 원래 요청 범위를 벗어난 것 같은 내용

보고서에 이런 항목이 있었습니다:
- `OptionsScreen` 패키지 분리
- **3D 캐릭터 렌더링 코드**

**우리가 원래 요청한 건 타이틀 화면(메인 메뉴) 배경/버튼/로고/눈 파티클이었지, 옵션 화면이나 3D 캐릭터 렌더링은 요청한 적이 없습니다.**

이건 두 가지 가능성이 있어요:
1. 작업하던 도구(에이전트)가 원래 마인크래프트 소스에 있던 기존 코드(타이틀 화면에 캐릭터가 서있는 3D 렌더링 같은 바닐라 기능)를 버전 포팅하는 과정에서 **자연스럽게 같이 옮긴 것** (문제 없음, 오히려 타당함 — 타이틀 화면엔 원래 캐릭터가 서 있으니까)
2. 아니면 **요청 범위를 벗어나 스스로 판단해서 기능을 추가/수정한 것** (이거면 의도치 않은 부분까지 건드렸을 수 있어서 위험)

**확인 요청**: `OptionsScreen`을 왜 건드렸는지, 우리가 만들고 있는 타이틀 화면 기능과 무슨 상관인지 `walkthrough.md`에서 설명하는 부분을 찾아서 그 이유를 알려주세요.

---

## 3. 실제 파일 구조를 직접 봐야 확실함

지금은 "보고서 텍스트"만 있고 실제 폴더 트리를 못 봤습니다. 다음 중 하나로 실제 구조를 보여주시면 정확하게 검증해드릴 수 있어요:
- 터미널에서 `git ls-files` 또는 `tree` 명령어 실행한 결과 복붙
- 또는 깃허브에 push한 뒤 저장소 링크 공유

---

## 4. 지금 당장 push 하기 전에 확인할 것
- [ ] `common` 모듈(또는 그에 준하는 공통 코드 공유 방식)이 실제로 존재하는지
- [ ] 두 버전 폴더의 `fabric.mod.json`이 각각 독립적으로 잘 구성되어 있는지 (버전마다 `depends`에 명시하는 마인크래프트 버전 범위가 정확한지)
- [ ] `.github/workflows/build.yml`이 **두 서브프로젝트를 각각 빌드**하도록 되어 있는지, 아니면 하나만 빌드하고 있는지
- [ ] `OptionsScreen`/3D 캐릭터 렌더링 관련 코드가 우리가 원래 만들려던 기능(타이틀 화면 테마+눈 파티클)과 충돌하지 않는지

---

## 5. 결론
구조 자체(버전별로 폴더 나누기)는 방향은 맞습니다. 근데:
- **공통 코드 공유가 안 되어 있다면 다시 손봐야 함** (제일 중요)
- **범위를 벗어난 변경(OptionsScreen 등)이 의도된 게 맞는지 확인 필요**

push 하기 전에, 실제 폴더 구조(`tree` 명령어 결과나 깃허브 링크)를 먼저 보여주시면 정확하게 검토해드릴게요.
